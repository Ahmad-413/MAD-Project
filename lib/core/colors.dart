import 'package:flutter/material.dart';

class AppColors {
  // ── Light Theme ──────────────────────────────────
  static const Color primary      = Color(0xFF111844);
  static const Color secondary    = Color(0xFF0D0B61);
  static const Color accent       = Color(0xFF36ADA3);
  static const Color lightAccent  = Color(0xFF789BD8);
  static const Color success      = Color(0xFF2CA960);
  static const Color darkSurface  = Color(0xFF134E4A);
  static const Color background   = Color(0xFFF5F5F5);
  static const Color cardLight    = Color(0xFFFFFFFF);
  static const Color textDark     = Color(0xFF111844);
  static const Color textLight    = Color(0xFFFFFFFF);
  static const Color textMuted    = Color(0xFF789BD8);
  static const Color textGrey     = Color(0xFF9E9E9E);

  // ── Dark Theme ───────────────────────────────────
  static const Color darkBg       = Color(0xFF0B1025);
  static const Color darkCard     = Color(0xFF1A224F);
  static const Color darkNav      = Color(0xFF111844);
  static const Color darkInput    = Color(0xFF1E2A5E);
  static const Color darkDivider  = Color(0xFF2A3570);
}